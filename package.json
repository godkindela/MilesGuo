{
  "name": "gwins-md",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "dev:scheduled": "wrangler dev --test-scheduled",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 execute gwins-md --file=src/schema.sql",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "wrangler": "^4.34.0",
    "@cloudflare/workers-types": "^4.20260208.0"
  }
}
